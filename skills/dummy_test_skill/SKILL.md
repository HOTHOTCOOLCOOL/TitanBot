# Dummy Test Skill

This skill is used exclusively for evaluating the Phase 56 Pre-flight Skill Verifier (PSV) mechanism.
It provides a baseline mechanism to test `validator.py` features such as AST restrictions, timeout cutoffs, and fail-open behaviors.
